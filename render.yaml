services:
  - type: web
    name: optic-reliability-platform
    runtime: static
    plan: free
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    previews:
      generation: automatic
    envVars:
      - key: NODE_VERSION
        value: "20.11.0"
